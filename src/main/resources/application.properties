spring.application.name=clss-search-evaluator

output.dir=${OUTPUT_DIR:output}
search.host=${SEARCH_HOST:http://localhost:8080}
search.auth-token=${SEARCH_AUTH_TOKEN:}
dataset.path=${DATASET_PATH:classpath:dataset/queries.json}
